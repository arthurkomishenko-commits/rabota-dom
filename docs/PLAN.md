# PLAN.md — Фазы производства Rabota Dom
Версия 1.0 · Размещение: `docs/PLAN.md` · Порядок строгий; фаза = свой манифест `docs/manifests/NN-*.md`; фаза не закрывается без выполненного гейта и отчёта с доказательствами (протокол §5, §7).

## F0 — Фундамент (манифест 01) · ✅ ГЕЙТ ПРОЙДЕН 07.08.2026
Доказательства: `docs/manifests/01-foundation.md` → «Проверка критериев приёмки».
Реальное устройство подтверждено скринами Артура (`docs/evidence/f0/phone/`).
Полировочный проход выполнен: 11/11, BUG-0002 и BUG-0003 закрыты.

Репозиторий, Astro + i18n (`/`=he RTL без префикса, /ru/, /en/, `/admin` вне i18n-матрицы), Tailwind logical, CSS-токены обеих тем + каскад темы (localStorage → система → light), Rubik self-host + сабсеттинг, базовый layout (dir/lang, hreflang + x-default), CI: build.
**Гейт:** билд чист; три локали открываются; RTL зеркалится; тема переключается каскадом; сабсеты ≤40 КБ.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F1 — Дизайн-система
Кнопка «матовый алюминий» (5 состояний), инпуты, тег материала, карточка-«паспорт объекта» (со «спилом» clip-path, RTL-зеркало), фокус-кольца, sticky-панель 3 каналов, диммер фото тёмной темы (ниже фолда).
**Гейт:** скрины всех состояний в обеих темах и обоих направлениях; контраст AA подтверждён.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F2 — Контент-модель
Zod-схемы: работа портфолио (контент-пакет брифа §4 как обязательный минимум, coords, метрики, review) + манифест слоёв (zIndex<50, mobile/reduced-варианты). Коллекции he/ru/en. CI: Zod-гейт + banned-phrases (EDITORIAL.md). Intake: запрос у Артура первых 8 работ и фото по ТЗ.
**Гейт:** невалидная запись валит билд; тестовая запись рендерится на 3 языках.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F3 — Статические страницы
Главная (10 экранов, hero пока статичный плейсхолдер), услуги ×3 (анти-шаблонные блоки «только при наличии ассета»), портфолио + фильтры + лайтбокс + «живой чертёж» (статичная версия), about, contact, privacy (+политика удаления отзывов), accessibility (הצהרת נגישות), 404 трёхъязычная.
**Гейт:** LHCI mobile ≥90 на всех страницах; тексты прошли EDITORIAL.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F4 — Worker и формы
Worker: plain text без parse_mode, honeypot, time-check ≥2.5 с, KV rate-limit, нормализация телефона + `^05\d{8}$`, метаданные (страница/язык/устройство), CORS. Формы: мини + полная, клиентское сжатие фото (~1920px/≤1 МБ ×3), `ctx.waitUntil()` для фото, состояния 429/500, постоянная кнопка WhatsApp-fallback (location.href), экран благодарности + «Отправить ещё».
**Гейт:** тестовая заявка и тестовый отзыв с фото реально пришли в Telegram — скрин от Артура.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F5 — Анимационная система
Примитивы (ScrollReveal, LayeredObject, SplitText-заголовки, счётчики, прочерчивание линий), hero-сборка по манифесту (покадровая режиссура брифа §3, стоп на 85–90%, болты 3–4), финальный жест «уровень + подпись», живой чертёж интерактивный, normalizeScroll + 100dvh, деградация-лестница, ритм секций.
**Гейт:** 60fps при CPU×4 на всех сборках; reduced-motion показывает финал мгновенно; RTL-направления зеркалятся; тест-грань «круто собралось» пройден.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F6 — Карта
/map: Leaflet по IntersectionObserver, фото-маркеры, hover-карточка, попап-паспорт, markercluster >15, jitter 50–100 м, тизер на главной.
**Гейт:** ≥6 точек — иначе /map и тизер скрыты из навигации; страница проходит свой перф-профиль.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F7 — Калькулятор
/calculator: island, `import rates from '../data/rates.json'`, две услуги, дефолты, guard-rails min/max («Нестандартный проект — посчитаем вручную»), чекбокс демонтажа, «Показать ориентир» по тапу, дисклеймер первым, «что чаще всего меняет стоимость», WhatsApp-префилл с параметрами и пометкой «ориентир с сайта».
**Гейт:** пока `status: draft` — режим «в калибровке», скрыт из меню; формулы покрыты тестами на граничные значения.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F8 — SEO, OG, запуск
Satori OG-паспорта (иврит: обязательный визуальный тест; фоллбеки Playwright → cover-фото), JSON-LD (LocalBusiness, Service, работы), sitemap, robots, аналитика + Search Console, финальный LHCI-прогон всех страниц.
**Гейт = Definition of Content Freeze (протокол §9):** ставки production · ≥8 паспортов · карта по правилу F6 · тексты вычитаны · гарантия согласована · формы подтверждены · OG-иврит ок → деплой и фиксация фриза в DECISIONS.md.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.

## F9 — Фаза 3.1 (после стабилизации запуска)
Decap CMS на /admin: готовый open-source OAuth-прокси на Worker, GitHub-аккаунт Владимира коллаборатором, авто-оптимизация медиа Action'ом после коммитов CMS, ветки CMS: Zod=error / LHCI=warn / auto-merge, строго типизированные поля. Роли: Владимир — проекты и отзывы; Артур — структура, схемы, дизайн, ставки.
**Гейт:** Владимир самостоятельно публикует тестовую работу с телефона; репозиторий не растёт неоптимизированными файлами.
**Полировочный проход (QUALITY_DOCTRINE §4):** перечитать весь дифф фазы, прогнать `npm run check`, пройти чек-лист edge-cases — пустые/минимальные данные · очень длинные строки · иврит и RTL · медленная сеть · отключённый JS · reduced-motion · узкий вьюпорт · клавиатурная навигация. Каждая находка → `docs/BUGS.md`, чинится по правилу первопричины до закрытия фазы.
