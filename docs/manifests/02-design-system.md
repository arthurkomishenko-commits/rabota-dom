# Манифест 02 — Дизайн-система (F1)

## Цель
Набор компонентов, из которых дальше собираются все страницы: кнопка, инпут,
тег материала, карточка-«паспорт объекта», sticky-панель трёх каналов, диммер фото.
Каждый компонент — с чистым швом: данные через props, ноль знаний о том, откуда
они пришли. Ни одной страницы в этой фазе не верстается.

## Связь с документами
Бриф §7 (токены, кнопка, инпуты, тег, фокус-кольца, диммер), §3 (sticky-панель
трёх каналов), §4 (карточка-паспорт: рамка 1px, фото 4:3 со «спилом» clip-path,
зеркалится в RTL, штамп monospace, метрики) · `docs/DESIGN_TOKENS.md` ·
ARCHITECTURE_PRINCIPLES §1, §2, §5 · QUALITY_DOCTRINE §3, §4 · протокол §5, §7.

## Что нужно от Артура (до старта)
1. **Ничего блокирующего.** Токены получены, доктрины узаконены.
2. К шагу 4 понадобится **одно тестовое фото 4:3** любого объекта — чтобы «спил»
   clip-path и диммер проверялись на реальном кадре, а не на сером прямоугольнике.
   ЗАЧЕМ: форма спила читается только на фотографии с содержанием.
   КАК: любой снимок из архива, JPEG, длинная сторона ≥1600px, в `docs/evidence/f1/`.
   Пока фото нет — шаг 4 идёт на плейсхолдере и помечается как неполный.

## Мини-план (QUALITY_DOCTRINE §3)

Каждый шаг: сборка компонента → доказательство (скрины всех состояний ×2 темы
×2 направления + цифры контраста) → запись факта сюда → следующий шаг.

1. **Витрина компонентов** — страница `/__ui/` (закрыта `noindex`, удаляется в F8).
   Единственное место, где компоненты показываются во всех состояниях сразу;
   на неё же нацелен скриншот-прогон. *Проверка:* страница собирается, в навигацию
   не входит, `check:conventions` зелёный.
2. **Кнопка «матовый алюминий»** — 5 состояний из `DESIGN_TOKENS.md` дословно
   (normal, hover, active, focus, disabled), обе темы. Варианты: primary и
   вторичная текстовая ссылка (бриф §3: иерархия CTA).
   *Проверка:* скрины 5 состояний ×2 темы; `disabled` не фокусируется с клавиатуры;
   фокус-кольцо видно в обеих темах.
3. **Инпут** — нижняя «линия чертежа» становится активной на focus, плейсхолдер
   токеном `--text-muted`, состояние ошибки. *Проверка:* скрины; ввод иврита
   выравнивается по RTL, номер телефона внутри ивритской формы — `dir="ltr"`.
4. **Тег материала** и **карточка-«паспорт объекта»** — рамка 1px, фото 4:3 со
   «спилом» clip-path, **зеркалится в RTL**, штамп monospace (название · город ·
   год) и метрики. Метрика без данных не рендерится вообще (бриф §4: фейки
   запрещены). *Проверка:* скрины he/ru/en ×2 темы; карточка с неполными данными
   не показывает пустых мест.
5. **Sticky-панель трёх каналов** (бриф §3) — WhatsApp · Позвонить · Заявка,
   равноправные, появляется со второго экрана, hero не перекрывает.
   *Проверка:* на 320px три цели не «слипаются», каждая ≥44px по короткой стороне;
   панель не накрывает контент внизу страницы.
6. **Диммер фото** `photo-dim` — довести до финального вида (DEC-0005).
   *Проверка:* применяется только к помеченным фото; в hero-контейнере `filter`
   отсутствует — проверяется вычисленным стилем, а не глазами.
7. **Контраст AA** — измерить пары токенов инструментом, приложить цифры.
   *Проверка:* отчёт с числами по каждой паре; `--accent-wood` на мелком тексте
   не используется (в `DESIGN_TOKENS.md` он помечен как «на грани AA для small text»).
8. **Полировочный проход F1** (QUALITY_DOCTRINE §4) — расширить `polish-pass.mjs`
   витриной компонентов: клавиатура по всем интерактивам, 320px, длинные строки
   в карточке, reduced-motion, без JS.

## Критерии приёмки

Общие для фазы:
- Скрины **всех** состояний **всех** компонентов: 2 темы × 2 направления.
- Контраст AA подтверждён **цифрами**, а не утверждением.
- `npm run check` и `npm run polish` зелёные.
- Полировочный проход выполнен, находки закрыты или зарегистрированы с планом.

Для **каждого** компонента отдельно:
- **ARCHITECTURE_PRINCIPLES §2:** данные только через props; ни `fetch`, ни
  `astro:content`, ни обращений к `window` вне выделенных islands; направление
  зависимостей не нарушено (`check:conventions` зелёный).
- **ARCHITECTURE_PRINCIPLES §5:** одна ответственность на файл, ориентир ≤200 строк;
  шапка-JSDoc «что делает · вход/выход · кто использует»; экспорты именованные.
- **§1:** второй похожий компонент не обобщается в общий базовый — обобщение
  только при втором **реальном** использовании, и это отдельное решение.
- **QUALITY_DOCTRINE §4:** компонент прошёл чек-лист edge-cases — пустые и
  минимальные данные · очень длинные строки · иврит и RTL · без JS ·
  reduced-motion · 320px · клавиатурная навигация.

## Риски
- **Обобщение раньше времени.** Кнопка и sticky-панель внешне похожи — соблазн
  сразу завести «базовый интерактив». §1 это запрещает: сначала два реальных
  использования, потом абстракция.
- **Карточка-паспорт — самый тяжёлый компонент фазы** (clip-path + RTL-зеркало +
  условные метрики). Делается последней из статических, на реальном фото.
- **Контраст `--accent-wood`.** Источник токенов сам предупреждает, что светлый
  вариант на белом на грани AA для мелкого текста. Если измерение это подтвердит —
  это не повод менять токен молча: вопрос к Артуру, решение в DECISIONS.

## Что НЕ входит в F1
Анимации (F5 — стилистику заранее не решаем, режиссура идёт через внешний контур,
протокол §11), реальные тексты (F3, после EDITORIAL и вычитки), данные работ
(F2), формы с отправкой (F4).

---

## Факты выполнения

### Шаг 1 — витрина компонентов · ✅ 07.08.2026

Способ изоляции — DEC-0011: витрина живёт в `src/showcase/`, маршрут инжектится
интеграцией только при `UI_SHOWCASE=1`. В production-сборке `/__ui/` **не существует**,
поэтому вопросы sitemap / hreflang / навигации отпадают вместе с маршрутом.

Факт — production-сборка:
```
generating static routes
  ├─ /admin/index.html · /en/index.html · /ru/index.html · /index.html
4 page(s) built
find dist -path "*__ui*"  →  пусто
✓ публичный контур: 10 файлов, витрины нет, noindex на месте
```

Факт — сборка с витриной (`npm run build:ui`):
```
[WARN] [rabota-dom:ui-showcase] Витрина /__ui/ включена — эта сборка не для публикации
  ├─ /__ui/index.html · /admin · /en · /ru · /index.html
5 page(s) built
```

Факт — негативный тест сторожа: если витрина всё-таки окажется в `dist/`,
`npm run check:surface` падает:
```
✗ запрещённый маршрут в публичной сборке: dist/__ui/index.html
Публичный контур нарушен: 1.   exit=1
```
Проверка встроена в `npm run build`, значит отрабатывает и в CI перед загрузкой
артефакта на Pages.

**Содержимое витрины на этом шаге** — только то, что уже реально существует:
палитра токенов и фокус-кольцо. Разделы компонентов добавляются своими шагами.
Каждый образец показан сразу в LTR и RTL (`src/showcase/Specimen.astro`), поэтому
зеркалирование видно на одном скрине.

Факт — значения токенов на витрине совпадают с `docs/DESIGN_TOKENS.md` дословно,
прочитаны из CSS в рантайме (не переписаны руками):
```
light: #f7f7f9 #fff #1e242b #64748b #c25e24 #a04a1a #cbd5e1
dark:  #121417 #1a1d23 #e2e8f0 #94a3b8 #e07a3e #f29054 #2d333b
```
Скрины: `docs/evidence/f1/ui-light.png`, `ui-dark.png` (`npm run shots:ui`).

Соответствие §5: `Specimen.astro` и `index.astro` имеют шапку-JSDoc
«что делает · вход/выход · кто использует», по одной ответственности на файл.
Соответствие §2: витрина заведена отдельным слоем в проверке границ
(`showcase → config, lib, data, components, layouts, styles`), направление проверяется машинно.
Соответствие §1: статический сервер вынесен в `scripts/lib/serve-dist.mjs` при
**втором** реальном использовании (полировка + скриншоты витрины), не раньше.

**Поправка к этой записи.** Первая её версия утверждала «`npm run check` зелёный».
Это было неверно: `astro check` выдавал одну ошибку (`process` в `astro.config.mjs`
без `@types/node`), а я смотрел на отфильтрованный `grep`-ом вывод, где строка
`- 1 error` в единственном числе под шаблон не попала. Поймал CI. Разобрано
в `docs/BUGS.md` → BUG-0004, включая вывод про процесс проверки.

Проверки после шага, с кодами возврата:
```
npm run check   → самотест 16/16 · astro check 0 errors/0 warnings/0 hints   exit=0
npm run build   → 4 страницы · ✓ публичный контур: витрины нет, noindex на месте  exit=0
npm run polish  → 11/11                                                       exit=0
```


### Шаги 2, 3, 5, 6 и 4 — компоненты · ✅ 08.08.2026

Собраны: `Button.astro` (primary + вторичная текстовая ссылка, 5 состояний
дословно из `DESIGN_TOKENS.md`) · `Field.astro` («линия чертежа» по нижней грани,
состояние ошибки, `numeric` для телефона) · `MaterialTag.astro` ·
`WorkCard.astro` (рамка 1px, фото 4:3 со «спилом», штамп monospace, метрики) ·
`StickyChannels.astro` (три равные доли).

Разделы витрины разнесены по файлам `src/showcase/sections/` — одна
ответственность на файл (§5). Каждый показан сразу в LTR и RTL.

**Соответствие §2:** все пять компонентов принимают данные только через props.
Ни `fetch`, ни `astro:content`, ни обращений к `window`. `StickyChannels` не знает
ни телефона, ни адреса WhatsApp — они придут из конфига в F3.
**Соответствие §1:** кнопка и sticky-панель внешне похожи, но общий «базовый
интерактив» НЕ заведён — второго реального повода к обобщению пока нет.
Обобщён только статический сервер скриптов, и ровно на втором использовании.

Факт — `clip-path` «спила» зеркалится (вычисленный стиль, не на глаз):
```
LTR: polygon(0px 0px, calc(100% - 40px) 0px, 100% 40px, 100% 100%, 0px 100%)
RTL: polygon(40px 0px, 100% 0px, 100% 100%, 0px 100%, 0px 40px)
```
Факт — диммер строго опт-ин (DEC-0005), тёмная тема:
```
.photo-dim img        → grayscale(0.15) contrast(0.9)
[data-hero-like] img  → none
```
Скрины: `ui-light.png` · `ui-dark.png` · `card-ltr.png` · `card-rtl.png` ·
`card-ltr-dark.png` в `docs/evidence/f1/`.

**Найден и починен BUG-0005** — в RTL метрика «100×50» отображалась как «50×100»,
то есть значение меняло смысл. Разбор и лечение — в `docs/BUGS.md`.

**Наблюдение (не баг), в работу F3:** читаемость «спила» зависит от кадра.
В LTR срез виден белым треугольником по небу, в RTL на этом же фото он приходится
на белую стену и сливается. Дорисовывать грань среза я не стал — этого нет
в брифе §4, и это вопрос к Артуру вместе с отбором кадров.

### Шаг 8 — полировочный проход F1 · ✅ 08.08.2026
`npm run polish:ui` → **18/18**, exit=0. Проверки витрины включаются только если
она есть в `dist`, поэтому production-прогон их не касается.
```
✓ клавиатура · кольцо видно на всех 33 интерактивах витрины
✓ клавиатура · отключённая кнопка не получает фокус
✓ 320px · витрина без горизонтальной прокрутки
✓ 320px · цели sticky-панели ≥44px по короткой стороне — минимум 52px из 6
✓ тёмная тема · photo-dim приглушает / в hero-подобном блоке фильтра нет
✓ без JS · витрина рендерится, карточки на месте
```

### Шаг 7 — контраст AA · ⛔ ГЕЙТ F1 НЕ ЗАКРЫТ, нужно решение Артура
Измерено, цифры и варианты — в `docs/DECISIONS.md` → «ОТКРЫТО · Контраст AA».
Тёмная тема проходит целиком; в светлой не проходят три текстовые пары,
плюс отдельный вопрос по границе поля ввода.
Токены не тронуты, порог не тронут, `check:contrast` намеренно не подключён
к `npm run check` и CI до решения.

## Статус фазы F1
Шаги 1–6 и 8 закрыты. **Гейт F1 открыт** — до решения по контрасту.
