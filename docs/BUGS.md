# BUGS.md

Формат записи: **дата** · **где** · **как воспроизвести** · **статус** (открыт / в работе /
закрыт + чем подтверждено). Закрытие без доказательства запрещено (протокол §7, §10).

---

## Открытых багов нет

Фаза F0 закрывается без известных дефектов.

Ниже — то, что **выглядело** как баг и было проверено; оставлено, чтобы не проверять заново.

### 07.08.2026 · `check:conventions` падал на собственном комментарии · закрыт
Скрипт проверки конвенций отмечал нарушение в `src/styles/global.css` — в комментарии,
который объясняет запрет соответствующей единицы, эта единица упоминалась текстом.
Правило отработало верно. Комментарий переформулирован без литерала, чтобы в кодовой
базе не появлялось исключений из правила.
Подтверждение: чистый прогон + негативный тест (временный файл с тремя нарушениями →
3 ошибки, exit=1; после удаления — снова чисто).
