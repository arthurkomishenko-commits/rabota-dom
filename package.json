{
  "name": "rabota-dom",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check:conventions && npm run check:budgets && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fonts:sync": "node scripts/sync-fonts.mjs",
    "check:conventions": "node scripts/check-conventions.mjs",
    "check:types": "astro check",
    "check": "npm run check:conventions && npm run check:budgets && npm run check:types",
    "check:budgets": "node scripts/check-budgets.mjs",
    "gate:f0": "node scripts/gate-f0.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.0",
    "tailwindcss": "^4.3.3"
  },
  "private": true,
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@fontsource-variable/rubik": "^5.3.0",
    "playwright": "^1.62.1",
    "typescript": "^6.0.3"
  }
}
